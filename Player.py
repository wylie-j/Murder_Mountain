from Person import Person

class Player(Person):
    def __init__(self, IMAGE_LOCATION, WIDTH, HEIGHT, X_COORD, Y_COORD, name):
        Person.__init__(self, IMAGE_LOCATION, WIDTH, HEIGHT, X_COORD, Y_COORD, name)

    def interact():
        pass